## Wanderlust 旅行規劃 App — 完整需求文件 v5

---

### 一、整體架構與設計系統

**設計語言（Wanderlust Design System）**
- 主色：Coral `#FF6B47`、Teal `#00C9A7`、Violet `#7C5CE4`、Sun `#FFB830`
- 字型：Fraunces（標題/展示）、Plus Jakarta Sans（介面文字）
- 間距：4px 基礎網格系統
- 圓角：xs 2px / sm 6px / md 10px / lg 14px / xl 20px / pill 999px
- 按鈕：Primary（Coral）、Teal、Violet、Ghost、Danger，含 sm/lg/pill 尺寸變體
- 所有彈出視窗：白底、明顯邊框與陰影、永遠最上層、背景不變暗、有 ✕ 關閉按鈕

**架構層級**
- 第一層：旅行列表（含全域提醒面板）
- 第二層：單一旅行，含五個分頁：行程 / 景點 / 住宿交通 / 記帳 / 行李

**命名規則**：有名稱顯示名稱，否則以「出發日期 目的地」自動命名

**資料持久化**：所有資料跨 session 儲存（localStorage）

---

### 二、旅行列表頁

- 顯示所有旅行卡片，含：名稱、日期區間、天數、主要幣別、當地幣別標籤
- 每張卡片有「✏️ 編輯」按鈕，可直接在列表頁修改旅行資訊（名稱、目的地、日期、貨幣）
- 新增旅行：彈出視窗，貨幣以下拉選單選擇

**全域提醒面板（旅行列表頁頂部）**
- 集中顯示所有旅行的提醒事件，按緊急程度排序
- 包含：🎫 搶票提醒（來自景點）、🔔 免費取消截止提醒（來自住宿與交通）
- 顏色區分：3天內紅色、7天內橘色、14天內綠色；顯示倒數天數
- 點擊可跳轉至對應旅行的對應分頁

---

### 三、旅行詳情頁（共用頂部列）

頂部操作列包含：← 返回、旅行名稱與日期、📋 備忘錄、⬇ 匯出、✏️ 編輯

**備忘錄**
- 點開才顯示，不直接呈現在頁面上
- 大型文字輸入區，可記錄緊急聯絡、匯率備忘、重要提醒等整趟旅程備注
- 資料隨時自動儲存

**匯出（⬇ 匯出）**
- 點擊後在新分頁開啟完整旅行報告（HTML 格式）
- 可於新分頁用 Cmd/Ctrl+P 列印為 PDF 離線使用
- 匯出內容包含：旅行資訊、備忘錄、完整行程（含景點詳情）、記帳統計表、支出明細

---

### 四、行程分頁

- 根據旅行日期自動產生 Day 1、Day 2…，每天可展開/收合
- **一鍵收合／展開**：單一按鈕，全部收合時顯示「⊞ 展開全部」，否則顯示「⊟ 收合全部」
- **景點 chip 面板**：顯示尚未加入行程的景點，可拖曳 chip 至各天；面板本身可拖曳移動位置（插入任意天之前），解決天數多時需滾動問題；已加入行程的景點自動從面板消失

**行程項目（一般）**
- 欄位：時間（24小時制）、名稱、備註
- 可編輯（✏️）、刪除、**拖曳排序**（⠿ 把手）
- 住宿與交通自動出現在對應日期（待訂紅色、免費取消綠色）

**行程項目（景點連結）**
- 顯示：名稱、營業時間、票價、地圖連結、官網連結、備註；不顯示資料來源
- 點擊 ✏️ 就地編輯：時間、備註、營業時間、票價、官網、地圖連結
- 儲存後同步回景點分頁原始資料
- 可拖曳排序（與一般項目混合排序）
- 標示「✏️ 含手動修改」提示

---

### 五、景點分頁

- 按**字母排序**（英文 A-Z，中文依注音 zh-TW localeCompare）
- **一鍵收合／展開**：預設收合，點名稱可單獨展開，頂部有切換全部的單一按鈕
- 展開顯示：營業時間、票價、地圖連結、官網連結、資料來源、備註
- 可勾選已去（名稱顯示刪除線）
- 可拖曳至行程分頁的景點 chip 面板

**新增景點**
- 彈出視窗，欄位：名稱、營業時間（24小時制）、成人票價、Google Maps 連結（可手動填）、官方網站、備註
- 批次新增（一行一個名稱）

**AI 查詢功能**
- 🔍 單筆查詢 / 🔍 全部查詢
- 優先使用已填的官網或 Google Maps 連結作為查詢依據
- 回傳：營業時間（純字串）、票價、地圖連結、官網、資料來源
- 手動修改票價後標示「(手動)」，重新查詢不覆蓋手動值
- 重新查詢以景點內已儲存連結為依據

**搶票提醒**
- 欄位：日期、時間（24小時制）、時區
- 彙整至首頁提醒面板

---

### 六、住宿交通分頁

**住宿欄位**：名稱、訂房平台（預設 Booking.com）、入住/退房日期與時間、免費取消截止日、金額、幣別、訂單編號、備註
- 狀態標籤：待訂（紅）、可免費取消（綠）、免費取消倒數（3天紅/7天橘/其餘綠）
- 填入金額後自動寫入記帳（分類：住宿）

**交通欄位**：類型、出發地、目的地、日期、出發/抵達時間（24小時制）、免費取消截止日、金額、幣別、訂單編號、備註
- 同住宿狀態標籤邏輯
- 填入金額後自動寫入記帳（分類：交通）

---

### 七、記帳分頁

- 多幣別支援，各幣別分開統計，不做匯率換算
- 住宿/交通自動帶入（標示「自動」，不可刪除）
- **清單模式**：逐筆顯示，含金額、幣別、分類、日期、備註、附件縮圖
- **統計表模式**：表格格式，列=幣別，欄=分類+總和

**新增支出**：金額、幣別（下拉）、分類（下拉）、日期（預設今天）、備註、📎 選取檔案、🖼️ 選取照片、📷 開啟相機

---

### 八、行李分頁

- 頂部進度條（Coral→Violet 漸層）
- 手動新增、打勾顯示刪除線
- **行李清單可拖曳排序**
- **模板管理**：可新增/刪除、拖曳排序、直接拖曳至清單新增（不重複）
- 建立新旅行時自動帶入模板

---

### 九、通用 UI 規範

- 所有時間欄位：24小時制
- 所有輸入框：明顯可見邊框（1.5px solid `#C8C8DC`）
- 日期欄位：支援日曆選擇器
- 貨幣欄位：下拉選單
- 彈出視窗：白底、最上層、背景不遮暗
- 資料持久化：localStorage 跨 session 儲存
- AI 查詢回傳物件型 hours 自動轉換為字串（防止渲染錯誤）
- localStorage 舊資料在載入時自動清理（sanitizeData）

---

*v5 · 2026-04-30 · Wanderlust Travel App*